{
  "name": "fer-fintech",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "git pull && nuxt generate && cp CNAME dist/CNAME && git add . && git commit -m 'deploy' && git push && git subtree push --prefix dist origin gh-pages"
  },
  "browserslist": [
    "defaults"
  ],
  "dependencies": {
    "@nuxtjs/svg": "^0.1.12",
    "bootstrap": "^4.6.0",
    "core-js": "^3.8.3",
    "gsap": "^3.6.0",
    "normalize.css": "^8.0.1",
    "nuxt": "^2.14.12"
  },
  "devDependencies": {
    "@nuxtjs/style-resources": "^1.0.0",
    "browserslist": "^4.16.3",
    "fibers": "^5.0.0",
    "sass": "^1.32.8",
    "sass-loader": "^10.1.1"
  }
}
